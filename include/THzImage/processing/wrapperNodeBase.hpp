// A node class on which all nodes that wrap another node can be based on
// Should create some sort of NullReader to avoid adding a functionality to imageRingBuffer
// for neither having a reader nor transformer to draw data from
